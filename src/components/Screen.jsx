import React from 'react';

function Screen(){

  const imageConstraints = {
    maxWidth: 200,
    maxHeight: 200,
  };

  return(
    <div style={imageConstraints}>
    </div>
  );
}

export default Screen;
